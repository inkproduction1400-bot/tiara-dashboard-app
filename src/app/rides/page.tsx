"use client";
import AppShell from "@/components/AppShell";

export default function Page() {
  return (
    <AppShell>
      <section className="tiara-panel h-full p-4">
        <h2 className="text-xl font-extrabold">送迎車の登録・編集</h2>
        <p className="text-xs text-muted mt-1">このページは後で実装されます。</p>
      </section>
    </AppShell>
  );
}
